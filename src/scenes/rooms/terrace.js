import Phaser from "phaser";

export class TerraceScene extends Phaser.Scene {
    constructor() {
        super({key: 'terrace'});

        console.log('state terrace', this);
    }

    create () {

    }
}
